import React from 'react'
import { Images } from '../../constants/images';
import './Banner.scss';

const Banner = () => {
  return (
    <div>
        <img src={Images.BG_BANNER} alt="bg-banner" />
    </div>
  )
}

export default Banner